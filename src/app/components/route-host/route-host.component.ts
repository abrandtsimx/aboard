import { Component } from '@angular/core';

/** Empty routed component so the router can match /share and /** without an outlet. */
@Component({
  selector: 'app-route-host',
  standalone: true,
  template: '',
})
export class RouteHostComponent {}
